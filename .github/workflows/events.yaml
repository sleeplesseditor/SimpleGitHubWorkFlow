name: Events Demo
on:
  pull_request:
   types:
    - opened
  workflow_dispatch:
jobs:
  output-info:
    runs-on: ubuntu-latest
    steps:
      - name: Output event details
        run: |
          cat <<EOF
          ${{ toJSON(github.event) }}
          EOF